/*const car = document.querySelector('.car');
const transito = document.querySelector('.transito');

const laco = setInterval(() => {
    const carposicao = car.offsetLeft;

    if (carposicao <= 120) {
        car.style.animation = 'none';
        car.style.left = `${carposicao}px`;
    }
}, 10);*/